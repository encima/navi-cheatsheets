% packages, linux, unix, cli

# List all globally installed NPM packages
npm list -g --depth=0

# List all globally installed Python packages
pip3 list

# List all rust packages
cargo install --list

# List all installed fedora packages
dnf list installed

# Update dnf
sudo dnf update --refresh -y
